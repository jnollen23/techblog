<div class="card col-9">
    <div class="card-header">
        <span>{{post.title}}</span><span>{{post.created_at}}</span>
    </div>
    <div class="card-body">
        {{post.body}}
    </div>
</div>